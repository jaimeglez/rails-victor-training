%h1 Listing user
%br 
= link_to content_tag(:i,' Create User', class: 'fa fa-plus fa-lg'), 
   new_admin_user_path, class: 'btn btn-success'
%table.table.table-striped
  %thead
    %tr
      %th User name
      %th Email
      %th{:colspan => "4"}
  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= user.email
        %td= link_to content_tag(:i,' Show', class: 'fa fa-eye'), 
              admin_user_path(user), class: 'btn btn-info'
        %td= link_to content_tag(:i,' Edit', class: 'fa fa-pencil-square-o'), 
              edit_admin_user_path(user), class: 'btn btn-warning'
        %td= link_to content_tag(:i,' Create article', class: 'fa fa-plus'), 
              new_admin_user_article_path(user), class: 'btn btn-success'
        %td= link_to content_tag(:i,' Show articles', class: 'fa fa-eye'), 
              admin_user_articles_path(user), class: 'btn btn-info'
        %td= link_to 'Destroy User', admin_user_path(user),
              method: :delete,
              data: { confirm: 'Are you sure delete this user?' },
              class: "btn btn-danger"
